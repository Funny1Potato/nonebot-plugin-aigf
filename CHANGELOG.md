# CHANGELOG

## 0.4.0-beta（基于 v0.3.2 的拟人化增强测试版）

> ⚠️ 这是 beta 测试版本，用于测试新功能。安装方式为覆盖安装。

### 架构变更（相较于原版 nyaturingtest）

- **移除 HippoRAG**：删除基于知识图谱的长期记忆检索系统（`hippo_mem.py`、`siliconflow_embeddings.py`），改为 LLM 自主管理记忆
- **移除情绪系统**：删除 VAD 三维情感模型（`emotion.py`、`impression.py`、`profile.py`）
- **移除三阶段对话流水线**：原版为"检索→反馈→对话"三阶段，每阶段各调一次 LLM；新版合并为单次 LLM 调用，同时完成对话决策和记忆管理
- **移除对话状态机**：原版有潜水/冒泡/对话三种状态，由 LLM 评估意愿 + 随机阈值决定；新版由 LLM 自行决定是否发言
- **触发机制变更**：原版每 5-10 秒随机延迟处理；新版为攒够一定数量消息或超时后触发（数量和时间均可配置，默认 10 条 / 30 秒）
- **移除 help 命令**：删除 `help` / `帮助` 命令（群聊和私聊）

### 新增功能（相较于原版 nyaturingtest）

#### 表情包系统
- **表情包发送**：AI 从预配置的表情包列表中选择发送
- **表情包自动收集**：群聊中的图片经 VLM 分析后缓存，LLM 决定是否收藏
- **双轨素材库**：管理员手动配置（`memes.json`）+ 自动收集（`collected.json`）
- **缓存机制**：图片到达后先缓存，LLM 处理后决定是否收藏
- **自动清理**：超过上限时优先清理最近未使用的表情包

#### 图片理解双模式
- **VLM 模式**（默认）：独立 VLM 分析图片
- **LLM 模式**：图片 base64 直接附在 LLM prompt 中

#### LLM 驱动的记忆系统
- **短期记忆**：对话摘要，LLM 可增删改，按群隔离
- **长期记忆**：事件、知识，LLM 可增删改，按群隔离
- **群友信息**：按 QQ 号全局共享，LLM 可增删改

#### 其他功能
- **回复上下文理解**：支持 reply 标记
- **消息合并发送**：文本和 @ 合并，表情包单独发送
- **默认预设**：首次运行自动生成可编辑的默认预设
- **Bot 回复记录**：bot 的回复存入最近消息
- **完善的日志系统**：DEBUG/INFO/SUCCESS 三级日志

### Beta 版新增特性（相较于 v0.3.2）

#### 🎭 社交能量系统
- 机器人拥有动态的"社交能量"（`social_energy`，范围 0.0~1.0，初始 0.7）
- 每次消息处理时自然恢复（向基线 0.6 靠拢）+ 随机漂移（±0.08）
- 回复后消耗能量：基础消耗 0.03 + 按回复文字长度增加（最多 0.15）
- 能量值注入 prompt，影响 LLM 的回复意愿和热情程度
- 5 档能量描述：精力充沛 / 状态不错 / 一般般 / 有点懒 / 不想说话
- 形成自然的"话多→累了→安静→恢复→又想聊"周期

#### 💬 回复风格自然化
- Prompt 新增"回复风格"段落，指导 LLM 像发微信一样口语化
- 鼓励使用网络用语（"哈哈"、"233"、"确实"、"6"等）
- 回复长度有变化：有时一两个字，有时几句话
- 允许敷衍回复，不再对每条消息都认真回复

#### 🧠 选择性回复
- 回复决策规则从硬编码改为能量感知
- 结合社交能量和话题兴趣度决定是否回复
- 能量低时可以选择不回复或简单敷衍

#### 📨 消息合并
- 新增 `_merge_consecutive()` 方法，合并同一用户在时间窗口内的连续消息
- 在 prompt 构建时对 `recent_messages` 和 `messages_chunk` 进行合并
- 解决群友分多次发送导致 LLM 看到碎片化上下文的问题

#### ⏱️ 智能触发延迟
- 超时触发时，检测最后两条消息是否来自同一用户且间隔在合并窗口内
- 若是，等待下一周期（1 秒）再触发，避免在连续发送中途处理

#### 🔍 联网搜索系统
- **两阶段模式**：LLM 先分析是否需要搜索，需要时调用搜索 API，再带搜索结果生成回复
- **Function Calling 模式**：LLM 自主决定是否调用搜索工具（需要模型支持 function calling）
- **Tavily API**（默认）：专为 AI 设计，返回格式友好
- **DuckDuckGo**（备选）：免费，无需 API key
- 可扩展架构，支持后续添加其他搜索 API

#### 🧠 文化记忆系统
- 第四层记忆，专门存储梗、网络用语、流行语
- 存储格式：`{term, meaning, context, usage_examples, first_seen, usage_count, source}`
- LLM 主动识别并提取新的文化词汇
- 通过搜索学到的梗会自动存入文化记忆
- 根据当前聊天内容匹配相关的文化知识注入 prompt
- 让机器人能够理解和跟上群内的梗，弥补大模型知识延迟

### 配置变更（相较于 v0.3.2）

| 新增配置项 | 说明 | 默认值 |
|-----------|------|--------|
| `AIGF_MERGE_WINDOW` | 消息合并时间窗口（秒） | `5.0` |
| `AIGF_SEARCH_ENABLED` | 是否启用联网搜索 | `false` |
| `AIGF_SEARCH_MODE` | 搜索模式: two_stage / function_call | `two_stage` |
| `AIGF_SEARCH_API` | 搜索API: tavily / duckduckgo | `tavily` |
| `AIGF_TAVILY_API_KEY` | Tavily API Key | 空 |
| `AIGF_SEARCH_MAX_RESULTS` | 最大搜索结果数 | `3` |

### 命令变更（相较于 v0.3.2）

| 命令 | 变更 |
|------|------|
| `status` / `状态` | 输出新增社交能量显示 |

### Prompt 变更（相较于 v0.3.2）

| 新增段落 | 说明 |
|---------|------|
| `## 回复风格` | 指导 LLM 口语化、简短、有变化地回复 |
| `## 当前状态` | 展示社交能量描述，影响回复决策 |
| `## 相关文化知识` | 展示当前聊天中涉及的文化词汇 |
| `## 搜索结果` | 展示搜索到的信息（仅搜索时） |

| 改写段落 | 说明 |
|---------|------|
| 回复决策规则 | 从硬编码 4 条规则改为能量感知的自然决策指导 |
| 记忆操作说明 | 新增文化记忆操作说明 |

### 文件变更（相较于 v0.3.2）

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `config.py` | 修改 | 新增消息合并和搜索相关配置项 |
| `search.py` | 新建 | 搜索客户端（Tavily + DuckDuckGo） |
| `client.py` | 修改 | 新增 `generate_response_with_tools()` 支持 Function Calling |
| `session.py` | 修改 | 社交能量、消息合并、搜索集成、文化记忆、prompt 改写 |
| `memory.py` | 修改 | 新增文化记忆管理方法 |
| `setup.py` | 修改 | 新增依赖 `tavily-python` |

### 依赖变更（相较于 v0.3.2）

```
新增：tavily-python
可选：duckduckgo-search（使用 DuckDuckGo 时需要）
```

### 兼容性

- 完全兼容 v0.3.2 的记忆数据、表情包数据、预设文件
- 配置项全部向后兼容，新增配置项均有默认值
- 覆盖安装后无需额外配置即可使用（搜索功能需手动启用）

---

## v0.3.2（作为基线）

> 以下为 v0.3.2 的变更记录，供参考。

### v0.3.2
- 优化消息发送逻辑

### v0.3.1
- 优化记忆操作
- 优化上下文理解能力

### v0.3.0
- 优化上下文理解能力
- 修改回复决策，使机器人更拟人
- 新增多项日志，便于维护
- 优化 VLM 理解能力
- 优化群友信息存储逻辑
- 改进记忆操作方式，便于 LLM 编辑

### v0.2.1
- 简单优化

### v0.2.0
- 按群聊分别存储记忆
- 改进 prompt

### v0.1.6
- 改进 prompt，使 AI 更拟人

### v0.1.5
- 插件发布
